# Claude Code Guidance

Use the skills in `skills/` as reusable task procedures. Prefer loading only
the relevant `SKILL.md` and any referenced files needed for the active request.

Run `python scripts/validate_skills.py` after changing skill frontmatter or
folder names.

